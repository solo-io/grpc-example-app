# Smoke Test

This test provides an example of how to call the grpc-example-app endpoints. Make sure you've started the grpc-example-app somewhere and provided the vars 
`BOOKS_ADDR` and `RECORDS_ADDR` which are the address:port of the `books` and `records` microservices, respectively.